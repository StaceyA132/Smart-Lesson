# Smart Lesson Backend

This is the FastAPI backend for the Smart Lesson project.

## Setup
- Create a virtual environment
- Install dependencies: `pip install -r requirements.txt`
- Run with `uvicorn main:app --reload`
